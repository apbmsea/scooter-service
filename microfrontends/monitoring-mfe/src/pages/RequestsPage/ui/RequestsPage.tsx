import RequestsList from '../features/ui/RequestsList';
import './RequestsPage.scss'

const RequestsPage = () => {
  return (
    <main className="requests-page">
      <RequestsList />
    </main>
  );
};

export default RequestsPage;
